# include<bits/stdc++.h>
vector<int> kthSmallLarge(vector<int> &arr, int n, int k)
{
	sort(arr.begin(), arr.end());

	vector<int> v;
	v.push_back(arr[k-1]);
	//reverse(arr.begin(), arr.end());
	v.push_back(arr[n-k]);

	return v;
}
